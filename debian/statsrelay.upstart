description "statsrelay server"

start on runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5
umask 022

script
    exec start-stop-daemon --start --quiet --chuid statsrelay --pidfile /var/run/statsrelay.pid \
        --exec /usr/bin/statsrelay \
        --make-pidfile

end script
